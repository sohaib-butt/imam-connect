import React from "react";
import { RefundList } from "@/components/AdminDashboard";

const page = () => {
  return (
    <div className="relative">
      <RefundList />
    </div>
  );
};

export default page;
